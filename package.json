{
  "name": "email-monitoring-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc",
    "wrangler:build": "wrangler build",
    "predeploy": "npm run build",
    "deploy": "npm run wrangler:build && wrangler publish"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
